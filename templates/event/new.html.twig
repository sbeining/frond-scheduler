{% extends 'base.html.twig' %}

{% block title %}New Event{% endblock %}

{% block body %}
    <div class="container">
        {{ include('event/_form.html.twig') }}

        <a class="btn btn-secondary float-left" href="{{ path('event_index') }}">back to list</a>
        <div style="clear: both" />
    </div>
{% endblock %}
